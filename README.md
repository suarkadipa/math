📁 math-practice/
├── index.html
├── style.css
└── js/
    ├── config.js   ← state global, CFG, helpers
    ├── sfx.js      ← Web Audio + splash animations
    ├── quiz.js     ← generate, render, review, check, streak, PIN
    ├── focus.js    ← Focus Mode
    ├── admin.js    ← Admin panel
    └── main.js     ← init, timer, theme, event listeners